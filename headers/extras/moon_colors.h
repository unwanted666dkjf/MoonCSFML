#ifndef MOON_CSFML_EXTRAS_MOON_COLORS_H
#define MOON_CSFML_EXTRAS_MOON_COLORS_H


#ifdef __cplusplus
extern "C" {
#endif


#define moon_Colors_Red(alpha) 		{255, 0, 0, ((unsigned char)(alpha))}

#define moon_Color_Green(alpha) 	{0, 255, 0, ((unsigned char)(alpha))}

#define moon_Color_Blue(alpha) 		{0, 0, 255, ((unsigned char)(alpha))}

#define moon_Colors_Gray(alpha) 	{128, 128, 128, ((unsigned char)(alpha))}

#define moon_Colors_White(alpha) 	{255, 255, 255, ((unsigned char)(alpha))}

#define moon_Colors_Black(alpha) 	{0, 0, 0, ((unsigned char)(alpha))}


#define moon_Colors_Aqua(alpha)             {0, 255, 240, ((unsigned char)(alpha))}

#define moon_Colors_Purple(alpha)           {139, 0, 255, ((unsigned char)(alpha))}

#define moon_Colors_DarkPurple(alpha)		{102, 0, 153, ((unsigned char)(alpha))}

#define moon_Colors_Scarlet(alpha)          {255, 36, 0, ((unsigned char)(alpha))}

#define moon_Colors_Emerald(alpha)          {0, 155, 119, ((unsigned char)(alpha))}

#define moon_Colors_Harlequin(alpha)        {68, 148, 74, ((unsigned char)(alpha))}

#define moon_Colors_BloodRed(alpha)         {120, 6, 6, ((unsigned char)(alpha))}

#define moon_Colors_FrostySky(alpha)        {173, 216, 230, ((unsigned char)(alpha))}

#define moon_Colors_LightSteel(alpha)       {176, 196, 222, ((unsigned char)(alpha))}

#define moon_Colors_AlizarinCrimson(alpha)  {227, 38, 54, ((unsigned char)(alpha))}

#define moon_Colors_Olive(alpha)            {128, 128, 0, ((unsigned char)(alpha))}

#define moon_Colors_Teal(alpha)             {0, 128, 128, ((unsigned char)(alpha))}

#define moon_Colors_Maroon(alpha)           {128, 0, 0, ((unsigned char)(alpha))}

#define moon_Colors_Navy(alpha)             {0, 0, 128, ((unsigned char)(alpha))}

#define moon_Colors_Coral(alpha)            {255, 127, 80, ((unsigned char)(alpha))}

#define moon_Colors_Indigo(alpha)           {75, 0, 130, ((unsigned char)(alpha))}

#define moon_Colors_Plum(alpha)             {142, 69, 133, ((unsigned char)(alpha))}

#define moon_Colors_Turquoise(alpha)        {64, 224, 208, ((unsigned char)(alpha))}

#define moon_Colors_Mustard(alpha)          {205, 171, 45, ((unsigned char)(alpha))}

#define moon_Colors_Peach(alpha)            {255, 218, 185, ((unsigned char)(alpha))}

#define moon_Colors_Lavender(alpha)         {230, 230, 250, ((unsigned char)(alpha))}

#define moon_Colors_Charcoal(alpha)         {54, 69, 79, ((unsigned char)(alpha))}

#define moon_Colors_Bisque(alpha)           {255, 228, 196, ((unsigned char)(alpha))}

#define moon_Colors_Ruby(alpha)             {155, 17, 30, ((unsigned char)(alpha))}

#define moon_Colors_Jade(alpha)             {0, 168, 107, ((unsigned char)(alpha))}

#define moon_Colors_SunsetOrange(alpha)     {253, 94, 83, ((unsigned char)(alpha))}

#define moon_Colors_Periwinkle(alpha)       {204, 204, 255, ((unsigned char)(alpha))}

#define moon_Colors_Mauve(alpha)            {224, 176, 255, ((unsigned char)(alpha))}

#define moon_Colors_Ochre(alpha)            {204, 119, 34, ((unsigned char)(alpha))}

#define moon_Colors_Seafoam(alpha)          {159, 226, 191, ((unsigned char)(alpha))}

#define moon_Colors_Cerulean(alpha)         {42, 82, 190, ((unsigned char)(alpha))}

#define moon_Colors_Bronze(alpha)           {205, 127, 50, ((unsigned char)(alpha))}


#define moon_Colors_TemperedIron(alpha)     {96, 96, 110, ((unsigned char)(alpha))}

#define moon_Colors_TemperedSteel(alpha)    {88, 100, 112, ((unsigned char)(alpha))}

#define moon_Colors_BrushedIron(alpha)      {74, 82, 87, ((unsigned char)(alpha))}

#define moon_Colors_Gold(alpha)             {255, 215, 0, ((unsigned char)(alpha))}

#define moon_Colors_MetallicGold(alpha)     {212, 175, 55, ((unsigned char)(alpha))}

#define moon_Colors_OldGold(alpha)          {181, 141, 55, ((unsigned char)(alpha))}

#define moon_Colors_RichGold(alpha)         {255, 204, 51, ((unsigned char)(alpha))}


#define moon_Colors_Silver(alpha)           {192, 192, 192, ((unsigned char)(alpha))}

#define moon_Colors_MetallicSilver(alpha)   {192, 192, 194, ((unsigned char)(alpha))}

#define moon_Colors_PolishedSilver(alpha)   {220, 220, 222, ((unsigned char)(alpha))}

#define moon_Colors_TarnishedSilver(alpha)  {160, 160, 162, ((unsigned char)(alpha))}


#define moon_Colors_Platinum(alpha)         {201, 203, 205, ((unsigned char)(alpha))}

#define moon_Colors_BrightPlatinum(alpha)   {226, 228, 229, ((unsigned char)(alpha))}

#define moon_Colors_CoolPlatinum(alpha)     {185, 190, 194, ((unsigned char)(alpha))}


#define moon_Colors_MoldGreen(alpha)        {102, 132, 88, ((unsigned char)(alpha))}

#define moon_Colors_DampMold(alpha)         {118, 142, 74, ((unsigned char)(alpha))}

#define moon_Colors_SporeGray(alpha)        {141, 153, 120, ((unsigned char)(alpha))}


#define moon_Colors_Moonlight(alpha)        {232, 236, 241, ((unsigned char)(alpha))}

#define moon_Colors_LunarGray(alpha)        {187, 191, 196, ((unsigned char)(alpha))}

#define moon_Colors_Moonshadow(alpha)       {120, 127, 135, ((unsigned char)(alpha))}


#define moon_Colors_SunCore(alpha)          {255, 170, 51, ((unsigned char)(alpha))}

#define moon_Colors_SolarFlare(alpha)       {255, 94, 25, ((unsigned char)(alpha))}

#define moon_Colors_Daylight(alpha)         {255, 223, 140, ((unsigned char)(alpha))}


#ifdef __cplusplus
}
#endif


#endif
